{{page-title "ember-data relationship tracking"}}

<h3>Ember data 4.5.0</h3>

{{#each this.observed as |item|}}
  {{item.name}}
{{/each}}

<button type="button" {{on "click" this.pushPayload}}>PUSH PAYLOAD</button>
